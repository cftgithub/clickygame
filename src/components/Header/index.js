import React from "react";
import "./style.css";

function Heading() {
    return (
        <div className="hero">
            <h1>Clicky Game</h1>
            <h2>Try to click all images only once to win!</h2>
        </div>
    );
}

export default Heading;